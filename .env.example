# Public frontend API base (no secrets). Used by: npm run build / npm start
# Local:
SOLMED_API_URL=http://localhost:8081

# Production (set this in Vercel Environment Variables):
# SOLMED_API_URL=https://solmedbackend-production.up.railway.app
