{
  "name": "solmed-frontend",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-api-config.js",
    "prestart": "node scripts/generate-api-config.js",
    "start": "npx --yes serve . -l 5000"
  }
}
